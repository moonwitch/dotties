#!/bin/sh

case $BLOCK_BUTTON in
	1) $TERMINAL -e ncpamixer & disown ;;
	2) scroll
	3) right
	4) pamixer -i 5;;
	5) pamixer -d 5;;
esac

printstatus() { [[ $(pamixer --get-mute) = "true" ]] && echo -n "MUTE" && exit
echo "VOL $(pamixer --get-volume)% )
printstatus
